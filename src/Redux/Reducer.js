const inicialState = {
    username:[]
}

const rootReducer = (state = inicialState, action) => {
    switch (action.type) {
        case USER:
            
            break;
    
        default:
            break;
    }
}